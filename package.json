{
  "name": "Dave's spacerocket tailwind tutorial",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/css/style.css --watch",
    "prettier": "npx prettier --write ./dist/index.html"
  },

  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "prettier-plugin-tailwindcss": "^0.1.13"
  }
}
